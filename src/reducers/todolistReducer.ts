export const todolistReducer = (state: any, action: ActionsType): any => {
   switch (action.type) {
      case "xxx": {
         return state;
      }
      default: {
         return state;
      }
   }
}


type ActionsType = any;